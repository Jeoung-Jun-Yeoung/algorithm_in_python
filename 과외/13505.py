# xor -> 두개가 다르면 1 같으면 0


# 해쉬를 하면 원본 데이터는 소실된다.
# 해쉬를 한 후에는 같은지 아닌지만 판단 가능.
def Trie():


def main():
    N = input()
    Number = list(map(int, input().split()))

    bitMap = []
    for num in Number:
        bit = num // 2
        bitMap.append(bit)
        # ^
